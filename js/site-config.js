/**
 * PixoPDF Site Configuration Registry
 * Host parameters, sitemap links, and site properties.
 */

export const SiteConfig = {
  SITE_NAME: "PixoPDF",
  
  // Production Cloudflare Pages URL.
  // UPDATE this configuration setting when custom domains are mapped to the deployment.
  SITE_URL: "https://pixopdf.online",
  
  SITE_DESCRIPTION: "Free online PDF tools to merge, split, compress and convert files directly in your browser. No signup, no watermark and zero file uploads."
};
